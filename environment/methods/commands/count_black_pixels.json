{
  "name": "count_black_pixels",
  "description": "Counts the number of black pixels in an image",
  "command": "python3",
  "script_path": "main.py",
  "parameters": [
    {
      "name": "threshold",
      "description": "Pixel value below which pixels are considered black (0-255)",
      "type": "integer",
      "required": false,
      "default": 30
    }
  ]
} 