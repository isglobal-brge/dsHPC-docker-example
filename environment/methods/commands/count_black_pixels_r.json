{
  "name": "count_black_pixels_r",
  "description": "Counts the number of black pixels in an image using R",
  "command": "Rscript",
  "script_path": "main.R",
  "parameters": [
    {
      "name": "threshold",
      "description": "Pixel value below which pixels are considered black (0-255)",
      "type": "integer",
      "required": false,
      "default": 30
    }
  ]
}
